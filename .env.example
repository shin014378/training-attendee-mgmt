# Neon（Vercel Storage からコピー）
# Vercel ダッシュボード → Project → Storage → Postgres → .env.local tab
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"

# マイグレーション / db push 用（Vercel が DATABASE_URL_UNPOOLED として提供）
DATABASE_URL_UNPOOLED="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"
