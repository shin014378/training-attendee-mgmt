# ローカル: Vercel Storage の .env.local タブからコピー
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"
DATABASE_URL_UNPOOLED="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"

# Vercel 本番: Storage を Connect すると POSTGRES_* が自動注入されます。
# scripts/sync-db-env.mjs が DATABASE_* に合わせます。
